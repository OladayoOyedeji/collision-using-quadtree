#ifndef FUNCTION_H
#define FUNCTION_H

#include "GameObject.h"

void slow_method(std::list<Ball * > & balls)
{
    for (std::list< Ball * >::iterator p = balls.begin();
         p != balls.end(); ++p)
    {
        for (std::list< Ball * >::iterator p = balls.begin();
             p != balls.end(); ++p)
        {
            
        }
    }
}

#endif
